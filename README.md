# Sherlock Style Detective Game

Multiplayer detective game with a React frontend and Node.js/Socket.io backend.

## Setup

```bash
npm install
```

## Run

In separate terminals:

```bash
npm run dev:backend
npm run dev:frontend
```

The backend creates a local SQLite database on first run.
